[build-system]
requires = ["uv_build>=0.12.1,<0.13"]
build-backend = "uv_build"

# NOTE: the requested distribution name `uni-rl` is unregistrable on PyPI
# (ultranormalized collision with the existing `unirl` project).
# Distribution name is therefore `unilab-rl`; the import namespace stays `uni_rl`.
[project]
name = "unilab-rl"
version = "1.2.0"
description = "Reinforcement learning algorithms and async runtimes extracted from UniLab (PPO/APPO/SAC/TD3/FlashSAC)"
readme = "README.md"
requires-python = ">=3.10,<3.14"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "UniLab contributors" }]
keywords = ["reinforcement-learning", "robotics", "gpu-simulation"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "numpy",
    "torch>=2.7.0",
    "tensordict",
    "omegaconf",
    "hydra-core",
    "rsl-rl-lib>=5.0.0",
    "tensorboard",
    "wandb>=0.22.3",
    "packaging",
    "rich",
]

[project.urls]
Repository = "https://github.com/unilabsim/unilab_rl"

[tool.uv.build-backend]
module-name = "uni_rl"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov",
    "ruff",
    "mypy",
    # Pinned: 1.1.411+ raises false positives against torch 2.8 stubs
    # (reportPrivateImportUsage on torch._foreach_*) that UniLab's gate avoids
    # by staying on 1.1.408.
    "pyright==1.1.408",
]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = [
    "E501",
    "N806",
    "E402",
    "F401",
    "N801",
    "F821",
    "E741",
    "N802",
    "N803",
    "N812",
]

[tool.mypy]
mypy_path = "src"
explicit_package_bases = true
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
no_site_packages = true

[tool.pyright]
pythonVersion = "3.10"
venvPath = "."
venv = ".venv"
include = ["src/uni_rl"]
reportMissingImports = "warning"
reportMissingModuleSource = "none"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: long-running training or smoke tests",
]
addopts = "--tb=short -m 'not slow'"
