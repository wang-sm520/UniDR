"""Algorithm implementations (runners, learners, collectors).

Runtime infrastructure (IPC, logging, off-policy runner scaffolding, shared
utils) deliberately lives top-level in ``uni_rl``; only the algorithm layer
is grouped under this subpackage.
"""
