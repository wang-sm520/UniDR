# @package _global_
# Fresh joint control after the four independent comparison policies.
defaults:
  - /task/g1_flip_tracking/unidr_single_gpu
  - _self_

algo:
  num_envs: 1024
  max_iterations: 5000
  resume: false
  resume_path: null
  load_run: "-1"
