# @package _global_
defaults:
  - /task/g1_flip_tracking/unidr
  - _self_

training:
  sim_backend: isaacgym

env:
  isaacgym_device_id: 0
  scene:
    entities:
      robot:
        # The subprocess backend exposes named bodies, not geometry IDs.
        geom_names: null
