# @package _global_
# Native --profile comparison owner.
defaults:
  - /task/g1_flip_tracking/unidr_isaacsim
  - _self_

algo:
  num_envs: 4096
  max_iterations: 5000
  resume: false
  load_run: "-1"
  resume_path: null

env:
  seed: 2
