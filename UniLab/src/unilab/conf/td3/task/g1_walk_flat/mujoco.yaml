# @package _global_
# TD3 MuJoCo owner: inherits the 29-DoF off-policy Manager-Based contract and
# only carries backend/algo identity.
defaults:
  - /task/g1_walk_flat/base
  - _self_

training:
  task_name: G1WalkFlat
  sim_backend: mujoco
algo:
  max_iterations: 100000
